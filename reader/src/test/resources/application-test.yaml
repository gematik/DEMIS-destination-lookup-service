logging:
  level:
    de.gematik.demis.destination.lookup: DEBUG
    org:
      testcontainers: DEBUG
    org.hibernate:
      SQL: DEBUG
      orm.jdbc.bind: trace

spring:
  liquibase:
    enabled: true
    change-log: classpath:db/changelog.yaml

  datasource:
    password: destination_lookup_reader
    username: destination_lookup_reader
